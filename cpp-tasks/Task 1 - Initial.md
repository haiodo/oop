# ФИТ НГУ, курс ООП. Задача 1 (раздельная компиляция и Word Count)

Цель: познакомиться с инструментами разработки C++ (Git, CMake, тесты), раздельной компиляцией и пространствами имен; решить задачу по обработке текста.

## Порядок действий

0. Пройти краткий курс по Git: https://learngitbranching.js.org (базовые вещи).
1. Зарегистрироваться на GitHub и создать ПРИВАТНЫЙ репозиторий. Предоставить доступ преподавателю для проверки.
2. Взять за основу шаблон курса: https://github.com/haiodo/oop-cpp-template
   (кнопка "Use this template" на GitHub). В нём уже настроены CMake, GoogleTest,
   все проверки и CI - см. его README.
3. Начать выполнение заданий.

## Требования к оформлению

1. Каждая подзадача - отдельная папка в `tasks/` (например, `tasks/task1a`, `tasks/task1b`)
   и отдельная ветка. Для проверки создайте Pull Request и добавьте преподавателя в ревьюеры.
2. Решение задачи 1b должно содержать тесты на GoogleTest. Для 1a тесты по желанию.
3. Проект собирается шаблоном курса, кроссплатформенно.
4. GitHub Actions настроен в шаблоне и включается сам.

---

## Задача 1a. Раздельная компиляция и пространства имен

Познакомьтесь с раздельной компиляцией и пространствами имен в C++ на примере:

Файл module1.h:

```c++
#include <string>

namespace Module1 {
  std::string getMyName();
}
```

Файл module1.cpp:

```c++
#include "module1.h"

namespace Module1 {
  std::string getMyName() {
    std::string name = "John";
    return name;
  }
}
```

Файл module2.h:

```c++
#include <string>

namespace Module2 {
  std::string getMyName();
}
```

Файл module2.cpp:

```c++
#include "module2.h"

namespace Module2 {
  std::string getMyName() {
    std::string name = "James";
    return name;
  }
}
```

Файл main.cpp:

```c++
#include "module1.h"
#include "module2.h"
#include <iostream>

int main(int argc, char** argv)
{
  std::cout << "Hello world!" << "\n";

  std::cout << Module1::getMyName() << "\n";
  std::cout << Module2::getMyName() << "\n";

  using namespace Module1;
  std::cout << getMyName() << "\n"; // (A)
  std::cout << Module2::getMyName() << "\n";

  //using namespace Module2; // (B)
  //std::cout << getMyName() << "\n"; // COMPILATION ERROR (C)

  using Module2::getMyName;
  std::cout << getMyName() << "\n"; // (D)
}
```

Что нужно сделать:

1. Собрать программу и убедиться, что на каждый .cpp создается отдельный объектный файл:
   - g++/Clang: .o
   - MSVC: .obj (например, в директории Debug)
2. Изменить один .cpp и пересобрать: должен обновиться только соответствующий ему объектный файл (у остальных дата изменения прежняя).
3. Объяснить, что выведется в местах (A) и (D) в main.cpp.
4. Раскомментировать строки (B) и (C) и убедиться, что возникает ошибка компиляции. Объяснить причину и предложить варианты исправления.
5. Добавить еще одну функцию getMyName(), возвращающую "Peter", в новом пространстве имен (например, Module3), и вывести результат в main.cpp.
6. Объяснить, как писать cout без std:: (и почему лучше избегать using namespace std; на уровне глобальной области видимости).

Подсказки и ожидаемое поведение:

- (A): после using namespace Module1; вызов getMyName() не квалифицирован и разрешается в пользу Module1::getMyName → вывод "John".
- (B)+(C): если раскомментировать using namespace Module2;, то не квалифицированный getMyName() станет неоднозначным между Module1 и Module2 → ошибка компиляции (ambiguity).
  Варианты исправления: вызывать явно Module1::getMyName()/Module2::getMyName(); убрать один из using namespace; либо использовать точечные using-директивы (например, using Module2::getMyName;) и понимать приоритет имен.
- (D): using Module2::getMyName; вводит именно эту функцию в текущую область видимости, поэтому не квалифицированный вызов выбирает Module2::getMyName → вывод "James".
- Добавление Module3:
  - Создайте module3.h/module3.cpp с функцией, возвращающей "Peter", и выведите Module3::getMyName() в main.cpp.
- Про cout:
  - Можно написать внутри функции main: using std::cout; и использовать cout.
  - Не рекомендуется писать using namespace std; на уровне заголовков/глобально — это увеличивает риск конфликтов имен.

---

## Задача 1b. Подсчет частот слов и вывод CSV

Написать программу, которая принимает имя входного текстового файла и имя выходного CSV-файла, и записывает в CSV три колонки:

1. слово, 2) частота, 3) частота в %.

Командная строка:

```bash
word_count input.txt output.csv
```

Требования:

- Кодировка входного файла - **однобайтовая** (ASCII, latin-1, cp1251, cp866). Один байт = один символ.
  Многобайтовые кодировки (UTF-8) в этой задаче не рассматриваются, таблиц кодировок писать не нужно.
- Слово - непрерывная последовательность байт, каждый из которых является ASCII-буквой (`A-Z`, `a-z`),
  ASCII-цифрой (`0-9`) или байтом `>= 0x80`. Любой другой байт - разделитель.
  Байты `>= 0x80` считаются буквами без разбора: в latin-1/cp1251/cp866 там как раз национальные алфавиты,
  а какая именно кодировка - программе знать не требуется.
- **Внимание:** `std::isalnum(c)` при `char c` со значением `>= 0x80` - это undefined behavior
  (аргумент должен быть представим как `unsigned char` либо равен `EOF`), а `char` на x86 знаковый.
  Прямая передача `char` в функции `<cctype>` - ошибка, а не стилистическая придирка.
  Либо приводите к `unsigned char`, либо не используйте `<cctype>` вовсе.
- Приведение регистра: **только ASCII** (`A-Z` -> `a-z`), чтобы "Word" и "word" считались одним словом.
  Байты `>= 0x80` не трогаем: без знания конкретной кодировки регистр национальных букв не определён.
  Значит "Слово" и "слово" - разные слова. Упрощение намеренное, в отчёте его надо назвать явно.
- CSV упорядочить по убыванию частоты (самые частые в начале). **При равной частоте - по возрастанию
  слова в лексикографическом порядке байт** (`std::string::operator<`). Порядок вывода обязан быть
  детерминированным: два запуска на одном входе дают побайтово одинаковый файл.
- Работа со строками — std::string.
- Работа с файлами — std::ifstream/std::ofstream из <fstream>.
- Чтение строк — std::getline.
- Данные:
  - Частоты хранить в `std::map<std::string, std::size_t>` или `std::unordered_map`.
  - Файл целиком в память загружать не нужно: читайте построчно и сразу учитывайте слова.
    Потребление памяти должно зависеть от числа **различных** слов, а не от размера файла.
- Подсчет процентов: `percent = 100.0 * count / total_words`, где `total_words` - **общее число вхождений
  слов** в файле (сумма всех частот), а не число различных слов. Формат - ровно 2 знака после запятой
  (`std::fixed` + `std::setprecision(2)`), десятичный разделитель - точка.
- Формат CSV: заголовок и затем строки вида:
  ```
  word,count,percent
  the,123,12.30
  ...
  ```

Подсказки:

- Парсинг: заменяйте не-алфа-нумерические символы на пробелы и затем разбивайте по пробелам.
- Сортировка для вывода: соберите пары (слово, частота) в вектор и отсортируйте по частоте по убыванию.
- Обработка ошибок: проверьте, что входной файл открылся; при ошибке - сообщите в stderr и верните ненулевой код.

Обязательно определите и реализуйте поведение для случаев:

- Пустой входной файл: выводится только строка заголовка `word,count,percent`, деления на ноль нет.
- Файл без единого слова (только знаки препинания) - то же самое.
- Неверное число аргументов командной строки - сообщение в stderr, код возврата 1.
- Входной файл не открылся - сообщение в stderr, код возврата 2.
- Слово длиной в тысячи байт и строка без единого перевода строки - работать должно.

---

## Сборка

Всё уже настроено в шаблоне. Для каждой подзадачи скопируйте папку `tasks/example`
и опишите её в `tasks/<имя>/CMakeLists.txt`:

```cmake
oop_task(NAME task1b SOURCES word_count.cpp MAIN main.cpp TESTS word_count_test.cpp)
```

Сборка и проверки:

```bash
cmake --preset dev  && cmake --build --preset dev  && ctest --preset dev
cmake --preset asan && cmake --build --preset asan && ctest --preset asan
```

Подробности - в README шаблона.

---

## Приём задачи

Общие правила - в [GRADING.md](GRADING.md). Задача 1 вводная: разговор по ней короткий,
но пункты ниже проверяются полностью.

### Автоматические проверки

```bash
cmake --preset dev  && cmake --build --preset dev  && ctest --preset dev
cmake --preset asan && cmake --build --preset asan && ctest --preset asan
```

Обе должны проходить без единого предупреждения. Покрытие для этой задачи не меряется.

### Зачёт

Задача 1a:

- [ ] На каждый `.cpp` создаётся отдельный объектный файл, это показано.
- [ ] После правки одного `.cpp` пересобирается только он: время изменения остальных не поменялось.
- [ ] Объяснено своими словами, что печатается в точках (A) и (D) и почему.
- [ ] Раскомментированные (B) и (C) дают ошибку компиляции; причина объяснена,
      предложен способ исправления.
- [ ] Добавлен Module3, его результат выводится.
- [ ] Объяснено, почему `using namespace std;` в заголовке опасен, а внутри функции допустим.

Задача 1b:

- [ ] Результат совпадает с эталоном на наборе преподавателя.
- [ ] Пустой файл и файл без слов не приводят к делению на ноль: печатается только заголовок CSV.
- [ ] Два запуска на одном входе дают побайтово одинаковый файл; тай-брейк по слову работает.
- [ ] Проценты считаются от суммы частот, 2 знака после запятой, сумма около 100.00.
- [ ] Текст в cp1251/cp866/latin-1 обрабатывается: байты `>= 0x80` не разрывают слова.
- [ ] `<cctype>` не вызывается с аргументом типа `char` напрямую.
- [ ] Коды возврата: 0 при успехе, 1 при неверных аргументах, 2 если файл не открылся;
      сообщения идут в `stderr`.
- [ ] Есть тесты на GoogleTest, включая разбор строки на слова и пустой вход.

### Автомат

Дополнительно к зачёту:

- [ ] Ключ `--top N`: вывести только N самых частых слов.
- [ ] Ключ `--stopwords <файл>`: перечисленные слова исключаются и из подсчёта,
      и из знаменателя процентов.
- [ ] Замер на файле от 100 МБ: программа работает, потребление памяти зависит
      от числа различных слов, а не от размера файла. Числа в `REPORT.md`.
- [ ] Сравнение `std::map` и `std::unordered_map` на этом файле: время работы обоих
      вариантов измерено и приведено в `REPORT.md` с объяснением разницы.

### Разговор: живая модификация

Одна на выбор преподавателя, около 10 минут:

1. Ключ `--min-count K`: не выводить слова с частотой меньше K.
2. Изменить тай-брейк на обратный лексикографический так, чтобы существующие тесты это поймали.
3. Считать не слова, а пары соседних слов (би-граммы).
4. Выводить дополнительную колонку: длина слова.
